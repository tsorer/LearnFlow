# Chatverlauf — Review PR #76 (T-39 OpenAPI als einzige Quelle für Backend und Frontend)

**Datum:** 2026-08-12
**Branch (Arbeitsverzeichnis):** `feat/T-44-user-facing-error` mit ungespeicherten Änderungen — Review-Gegenstand ist der fremde Branch `feat/T-39-openapi-types`, geprüft in einem separaten Worktree
**Gegenstand:** [PR #76](https://github.com/tsorer/LearnFlow/pull/76) „feat/T-39-openapi-types: OpenAPI als einzige Quelle für Backend und Frontend" (Issue [#58](https://github.com/tsorer/LearnFlow/issues/58))
**Ergebnis:** Review als Kommentar gepostet, **kein Approve** — State `COMMENTED`, Autor `FraMo80`, 2026-08-12T18:27:46Z

> **Abweichung von der Konvention dieses Ordners:** Die übrigen Dateien hier geben Nutzer-Prompts wörtlich und Claude-Antworten als Zusammenfassung wieder. Dieses Protokoll ist auf ausdrücklichen Wunsch **wortgetreu** — beide Seiten im Originalwortlaut. Ausgeführte Werkzeugaufrufe sind nicht Teil des Wortlauts; wo sie zum Verständnis nötig sind, stehen sie als kursive `[Aktion: …]`-Zeile dazwischen und sind damit als redaktionelle Ergänzung erkennbar.

---

## 1 — Nutzer

> /code-review https://github.com/tsorer/LearnFlow/pull/76

### Claude

_[Aktion: PR-Metadaten und Diff via `gh` und `git diff origin/main...origin/feat/T-39-openapi-types`; umgebender Code konsequent vom PR-Branch gelesen (`git show origin/feat/T-39-openapi-types:…`): `client.ts`, `schema.d.ts`, `types.ts`, `ChatView.tsx`, `MessageBubble.tsx`, `Login.tsx`, `Upload.tsx`, `App.tsx`, `session.test.tsx`, `main.py`, `auth/dependencies.py`, `routers/documents.py`, `models/tables.py`, `test_rbac.py`, `nginx.conf`, `docker-compose.yml`, CI-Workflow. Befunde via `ReportFindings` gemeldet.]_

## Findings — [PR #76](https://github.com/tsorer/LearnFlow/pull/76) (`feat/T-39-openapi-types`)

- [src/backend/tests/test_rbac.py:223](src/backend/tests/test_rbac.py:223) — the "nothing escapes the spec checks" guard only walks top-level `app.routes`; a `Route`/`Mount` registered on an *included* router sits behind the `_IncludedRouter` wrapper (excluded by `hasattr(route, "original_router")`) and is also dropped by `_iter_api_routes`' `isinstance(original, APIRoute)` filter — served, unauthenticated, undeclared, suite green.
- [src/frontend/src/components/MessageBubble.tsx:353](src/frontend/src/components/MessageBubble.tsx:353) — moving `setFeedback` after the `await` removes the in-flight guard; a double-click fires two POSTs, which becomes two (possibly contradictory) `feedback` rows once T-30 persists.
- [src/Makefile:64](src/Makefile:64) — `[ -x node_modules/.bin/openapi-typescript ] || npm ci` never reinstalls into the persistent named volume after a lockfile bump, so `make generate-api` writes types from a stale generator and CI's fresh `npm ci` then rejects them, telling the dev to run the command they just ran.
- [src/frontend/scripts/check-api-types.mjs:71](src/frontend/scripts/check-api-types.mjs:71) — comments are stripped before string literals, so a `//` inside a string truncates its closing quote; the quote-pairing regex on line 72 then mis-pairs and can blank out a span of `client.ts`, hiding a real bare `fetch(` from the guard.
- [src/frontend/src/components/ChatView.tsx:51](src/frontend/src/components/ChatView.tsx:51) — `saveParams` shows "✓ Gespeichert" even when `updateConfig` rejects (now guaranteed: PUT `/api/admin/config` returns 501). Line is unchanged by the PR, but it is the same latch bug the PR deliberately fixed in `MessageBubble`.

Things I checked that are **not** problems: `root_path="/api"` is safe with nginx's `rewrite ^/api/(.*)$ /$1` and with the direct `:8000/health` healthcheck (Starlette's `get_route_path` only strips a prefix that is actually present); `EXPOSE_API_DOCS` does reach the container via `env_file: .env`; the new `FeedbackRequest` matches the `feedback` table (`helpful`/`category`/`comment`); openapi-fetch drops `Content-Type` for the `FormData` serializer so the multipart upload keeps its boundary; and `unwrap`'s `!result.response.ok` branch correctly covers the empty-body error case where openapi-fetch returns `error: undefined`.

---

## 2 — Nutzer

> aud deutsch bitte

### Claude

## Befunde — [PR #76](https://github.com/tsorer/LearnFlow/pull/76) (`feat/T-39-openapi-types`)

- [src/backend/tests/test_rbac.py:223](src/backend/tests/test_rbac.py:223) — Der Wächter „nichts entkommt den Spec-Checks" durchläuft nur die oberste Ebene von `app.routes`. Eine `Route`/`Mount`, die auf einem *eingebundenen* Router registriert wird, sitzt hinter dem `_IncludedRouter`-Wrapper (durch `hasattr(route, "original_router")` ausgeschlossen) und fällt zusätzlich durch den `isinstance(original, APIRoute)`-Filter in `_iter_api_routes`. Ergebnis: ausgeliefert, unauthentifiziert, undeklariert — Suite grün. Genau das Loch, das der Docstring zu schliessen behauptet; die Mutationsprobe im PR deckte nur den Top-Level-Fall `app.add_route("/metrics")` ab.

- [src/frontend/src/components/MessageBubble.tsx:353](src/frontend/src/components/MessageBubble.tsx:353) — `setFeedback` hinter das `await` zu ziehen entfernt die Sperre gegen parallele Absendungen: Während der Anfrage ist `feedback` weiterhin `null`, der Check `feedback !== null` greift nicht. Ein Doppelklick (oder 👍 dann 👎) schickt zwei POSTs. Heute folgenlos wegen 501 — sobald T-30 persistiert, sind das zwei, ggf. widersprüchliche Zeilen zur selben `answer_id`. Ein eigenes `submitting`-Flag hält beide Eigenschaften.

- [src/Makefile:64](src/Makefile:64) — `[ -x node_modules/.bin/openapi-typescript ] || npm ci` installiert nach einer Lockfile-Änderung nie neu, weil das benannte Volume die alte Binärdatei behält. Nach einem Versionssprung erzeugt `make generate-api` die Typen mit dem alten Generator, die CI regeneriert mit frischem `npm ci` und lehnt sie ab — mit genau dem Befehl in der Fehlermeldung, den die Entwicklerin gerade ausgeführt hat, und das bleibt so, bis das Volume von Hand gelöscht wird. Besser gegen einen Lockfile-Hash prüfen statt gegen die Existenz der Binärdatei.

- [src/frontend/scripts/check-api-types.mjs:71](src/frontend/scripts/check-api-types.mjs:71) — Kommentare werden vor String-Literalen entfernt. Ein `//` innerhalb eines Strings (nicht von `:` vorangegangen) schneidet die Zeile inklusive schliessendem Anführungszeichen ab; die Quote-Paarung in Zeile 72 paart dann falsch und leert einen grösseren Bereich von `client.ts`. Ein echtes `fetch(` in diesem Bereich verschwindet, bevor Zeile 74 prüft — der Wächter läuft grün durch. Strings zuerst strippen (oder tokenisieren) beseitigt die Reihenfolgeabhängigkeit.

- [src/frontend/src/components/ChatView.tsx:51](src/frontend/src/components/ChatView.tsx:51) — `saveParams` zeigt „✓ Gespeichert" auch wenn `updateConfig` fehlschlägt (`.catch(() => {})`), was durch den neuen 501-Platzhalter auf `PUT /api/admin/config` jetzt garantiert der Fall ist: Admin glaubt, die Fail-closed-Schwellen (ADR-008) geändert zu haben, die `config`-Tabelle bleibt unberührt. Die Zeile ist vom PR nicht verändert — aber es ist die letzte verbliebene Stelle mit genau dem Fehlerbild, das der PR in `MessageBubble` bewusst beseitigt hat.

**Geprüft und in Ordnung:** `root_path="/api"` ist mit nginx' `rewrite ^/api/(.*)$ /$1` und mit dem direkten Healthcheck auf `:8000/health` unkritisch (Starlettes `get_route_path` entfernt nur ein tatsächlich vorhandenes Präfix); `EXPOSE_API_DOCS` erreicht den Container über `env_file: .env`, die README-Anweisung stimmt; das neue `FeedbackRequest` passt zur Tabelle `feedback` (`helpful`/`category`/`comment`); openapi-fetch entfernt `Content-Type` bei einem `FormData`-Serializer, der Multipart-Upload behält also seine Boundary; und der Zweig `!result.response.ok` in `unwrap` fängt korrekt den Fall ab, in dem openapi-fetch bei leerem Fehler-Body `error: undefined` zurückgibt.

---

## 3 — Nutzer

> Wie schwerwiegend sind die Befunde? Was passiert, es es nicht angepasst wird?

### Claude

## Gesamtbild

Keiner der fünf Befunde macht heute etwas kaputt. Alle sind **latent**: vier schlagen erst zu, wenn ein bestimmtes Folge-Ticket landet oder jemand eine bestimmte Änderung macht; einer betrifft nur die Wirksamkeit eines selbst gesetzten Wächters. Aus meiner Sicht ist der PR damit mergefähig — die Frage ist nur, was davon man für 20 Minuten gleich mitnimmt und was ein Folge-Ticket wird.

| # | Befund | Wirkt heute? | Scharf ab | Folge, wenn nichts passiert |
|---|---|---|---|---|
| 5 | ChatView „✓ Gespeichert" | nein (Panel ist ohnehin tot) | **T-37** | Admin ändert eine Fail-closed-Schwelle (ADR-008), bekommt grünes ✓, `config` bleibt unverändert. Betrieb mit angenommener, aber nicht gesetzter Schwelle |
| 2 | Feedback-Doppelklick | nein (501) | **T-30** | Zwei Zeilen pro Antwort, bei 👍/👎 widersprüchlich → verfälschte US-03-Auswertung. Kein Datenverlust, aber unbrauchbare Statistik |
| 1 | Blindstelle im Routen-Check | nein | wenn jemand `Route`/`Mount` auf einem eingebundenen Router registriert | Ein unauthentifizierter, undeklarierter Endpunkt geht durch die CI. Unwahrscheinlich (niemand macht das hier), aber genau der Fall, den der Test verspricht abzudecken |
| 4 | Kommentar/String-Strip-Reihenfolge | nein | sobald ein `//` in einem String in `client.ts` steht | Der Bare-fetch-Wächter läuft still grün. Begrenzt: `tsc` + openapi-fetch typisieren die Pfade weiterhin — es fällt nur die zweite Schicht weg |
| 3 | `generate-api` installiert nicht neu | nein | beim nächsten Bump von `openapi-typescript` | Rote CI mit einer Fehlermeldung, die zum gerade ausgeführten Befehl rät. Kostet eine Debugging-Runde, bis jemand auf `docker volume rm learnflow-fe-node-modules` kommt. Kein Produktionsrisiko |

## Was ich wo machen würde

**In diesem PR** (zusammen keine halbe Stunde, alles Einzeiler):
- **#5** — `try/catch` um `updateConfig`, `setParamSaved(true)` nur im Erfolgsfall. Es ist exakt der Fehler, den der PR in `MessageBubble` bewusst behoben hat; ihn drei Dateien weiter stehen zu lassen, ist inkonsistent. Von den fünf hat er die schlimmste Konsequenz, weil es um die ADR-008-Schwellen geht.
- **#2** — ein `submitting`-Flag. Sonst muss es sich jemand in T-30 merken, und dort fällt es nicht auf, weil der Endpunkt dann ja funktioniert.
- **#4** — Strings vor Kommentaren strippen, zwei Zeilen tauschen.

**Als Folge-Ticket oder bewusstes Restrisiko:**
- **#3** — entweder gegen einen Lockfile-Hash prüfen, oder einfach den `docker volume rm`-Hinweis in die README/den Makefile-Kommentar. Die billige Variante reicht.
- **#1** — der einzige mit echtem Aufwand (rekursiv durch `original_router.routes` statt nur oberste Ebene) und der geringsten Eintrittswahrscheinlichkeit. Vertretbar, das als bekannte Lücke im Docstring zu vermerken statt jetzt zu lösen.

Einschränkung: Das ist eine statische Bewertung — Docker und Node liefen hier nicht, ich habe die Suite nicht selbst ausgeführt. Die Verifikationsangaben im PR-Text (124 pytest, 8/8 e2e, 11/11 vitest) habe ich nicht nachgestellt.

---

## 4 — Nutzer

> docker läuft nun

### Claude

Gut — dann verifiziere ich die Befunde statt sie nur zu behaupten. Zuerst der Ist-Zustand.

_[Aktion: `git status` zeigte den Arbeitsbaum auf `feat/T-44-user-facing-error` mit acht geänderten Dateien — deshalb kein Branch-Wechsel, sondern ein separater Worktree unter `%TEMP%\claude\lf-pr76`.]_

Der Arbeitsbaum ist **nicht** sauber — du bist auf `feat/T-44-user-facing-error` mit ungespeicherten Änderungen. Ich fasse den nicht an und lege den PR-Branch in einem separaten Worktree ab.

_[Aktion: Backend-Suite im `src-api`-Image gegen den Worktree — 124 Tests grün.]_

124 Tests grün auf dem PR-Branch — die Angaben im PR stimmen. Jetzt die Mutationsprobe für Befund #1.

_[Aktion: `Route("/internal/reindex", …)` an `documents.router.routes` angehängt, Probe-Skript mit `TestClient`. Erste Probe traf den falschen Pfad (`/documents/internal/reindex` → 404), zweite Probe traf.]_

Falscher Pfad in meiner Probe — der Prefix greift bei `routes.append` nicht.

Befund #1 ist bestätigt: `GET /internal/reindex → 200`, ohne Auth, nicht in der Spec — und die Suite blieb im selben Lauf grün (124 passed). Jetzt Befund #4.

_[Aktion: Regex-Kette aus `check-api-types.mjs` in einem Node-Skript gegen ein synthetisches `client.ts` nachgestellt, mit Gegenprobe.]_

Bestätigt, exakt wie vorhergesagt. Jetzt noch Befund #3 empirisch, dann räume ich auf.

_[Aktion: Mutation zurückgenommen; zwei Container-Läufe mit benanntem Volume `learnflow-probe-nm`, dazwischen Lockfile-Bump auf 7.14.0. Danach Volume und Worktree entfernt, `git status` unverändert.]_

Bestätigt. Aufräumen.

Alles auf dem PR-Branch verifiziert, in einem separaten Worktree — dein Arbeitsbaum auf `feat/T-44-user-facing-error` ist unangetastet, Worktree und Probe-Volume sind wieder weg.

## Ergebnis der Verifikation

| # | Befund | Status | Beleg |
|---|---|---|---|
| — | PR-Angaben | **stimmen** | 124 Tests grün im `src-api`-Image; `effective_route_contexts()` existiert in der installierten FastAPI |
| 1 | Blindstelle im Routen-Check | **bestätigt** | `GET /internal/reindex → 200`, ohne Auth, nicht in der Spec — im selben Lauf 124 Tests grün |
| 4 | Regex-Reihenfolge im Wächter | **bestätigt** | mit `//` im String: 0 Treffer (echtes `fetch(` durchgerutscht); Gegenprobe ohne `//`: 1 Treffer |
| 3 | `generate-api` installiert nicht neu | **bestätigt** | Lauf 1: Lockfile 7.13.0 → installiert 7.13.0. Nach Lockfile-Bump auf 7.14.0: „npm ci wird übersprungen", installiert bleibt 7.13.0 |
| 2, 5 | Doppelklick / falsches ✓ | **nicht empirisch geprüft** | beides sind direkte Code-Lesungen (`setFeedback` hinter dem `await`; `.catch(() => {})` + unbedingtes `setParamSaved(true)`) — da gab es keine Hypothese zu testen |

Eine Korrektur zu Befund #1: Die geschmuggelte Route landet auf `/internal/reindex`, **nicht** unter dem `/documents`-Prefix des Routers — `routes.append` umgeht `add_route` und damit den Prefix. Meine erste Probe traf deshalb ins Leere. Am Kern ändert das nichts: die Route wird ausgeliefert, ist unauthentifiziert, steht nicht in der Spec, und alle drei Konformitätstests bleiben grün.

Bei #4 sieht man den Mechanismus schön: aus

```
const HINT = "nutze api.query // nicht den Endpunkt direkt";
const r = await fetch("/api/query", ...);
```

wird nach dem Strippen `const HINT = ""/api/query""POST" });` — das öffnende Anführungszeichen paart sich mit dem nächsten weiter unten, und `await fetch(` verschwindet mitsamt dem Zwischenraum.

## Was das an der Einschätzung ändert

Wenig — alle drei bleiben latent, keiner bricht heute etwas. Aber sie sind jetzt belegt statt vermutet, und das verschiebt zwei Dinge:

- **#3** ist kein Randfall mehr, sondern reproduzierbar in zwei Läufen. Beim nächsten Bump von `openapi-typescript` verliert jemand garantiert eine Debugging-Runde. Der Fix ist billig (gegen einen Lockfile-Hash prüfen statt gegen `-x`), notfalls reicht ein `docker volume rm learnflow-fe-node-modules` im Makefile-Kommentar.
- **#1** würde ich weiterhin als Folge-Ticket führen, aber der Docstring sollte in diesem PR ehrlich werden: er verspricht „Nothing reaches the network that the two checks below cannot see", und das stimmt nachweislich nicht. Entweder rekursiv durch `original_router.routes` gehen oder die Lücke benennen.

Meine Priorität für diesen PR bleibt: **#5 und #2** (Einzeiler, echte Nutzerwirkung ab T-37/T-30), **#4** (zwei Regex-Zeilen tauschen), **#3** als Kommentar oder kleiner Fix, **#1** als Ticket.

---

Nebenbei aufgefallen, gehört nicht zu PR #76: `Src/backend/app/exceptions.py` liegt untracked unter **`Src`** statt `src`. Das ist genau die Falle, in die `git add src/...` lautlos läuft.

---

## 5 — Nutzer

> OK, erstelle den PR-Review. Erwähne, dass es unkritsche Punkte sind, aber einfach zu korrigieren. Empfehlung für #1 und #3: Kommentieren und Restrisiko akzeptieren.

### Claude

Ich schreibe den Review-Text und gebe dir den Befehl zum Posten — nach deiner sonstigen Arbeitsweise (PRs/Pushes machst du selbst) poste ich ihn nicht ungefragt.

_[Aktion: Review-Text nach `scratchpad/review-pr76.md` geschrieben und als Datei übergeben.]_

Der Review-Text steht. Aufbau:

- **Gesamturteil: kein Blocker**, ausdrücklich als unkritisch gekennzeichnet — mit dem, was ich nachgestellt habe, damit die Punkte nicht als Behauptung dastehen.
- **In diesem PR mitnehmen** (die drei Einzeiler): `ChatView.tsx:51`, `MessageBubble.tsx:353`, `check-api-types.mjs:71`.
- **Kommentieren und Restrisiko akzeptieren**: `test_rbac.py:223` (Docstring ehrlich machen, statt eine Deckung zu versprechen, die er nicht hat) und `Makefile:64` (`docker volume rm learnflow-fe-node-modules` in den Kommentar).
- **Geprüft und in Ordnung** am Schluss, damit sichtbar ist, was ich angeschaut und *nicht* beanstandet habe.

Posten überlasse ich dir — du machst PRs und Pushes sonst selbst, und ein Review geht an die Reviewer raus:

```bash
gh pr review 76 --repo tsorer/LearnFlow --comment --body-file "…/scratchpad/review-pr76.md"
```

Zwei Dinge dazu:

`--comment` postet als neutralen Review-Kommentar. Wenn du stattdessen freigeben willst, `--approve` — das passt hier inhaltlich, weil keiner der Punkte mergeblockierend ist. Sag Bescheid, wenn ich es selbst absetzen soll oder als `--approve` umschreiben.

Und falls die drei Einzeiler lieber gleich als Commit statt als Review-Kommentar landen sollen: die sind zusammen keine halbe Stunde, aber das wäre ein eigener Branch — dein Arbeitsbaum hängt gerade an `feat/T-44-user-facing-error` mit ungespeicherten Änderungen.

---

## 6 — Nutzer

> poste es als Kommentar (noch kein Approve)

### Claude

_[Aktion: `gh pr review 76 --repo tsorer/LearnFlow --comment --body-file …`, anschliessend über `gh pr view 76 --json reviews` verifiziert.]_

Gepostet und verifiziert: Review von `FraMo80`, State `COMMENTED`, kein Approve.

[PR #76](https://github.com/tsorer/LearnFlow/pull/76)

Der Review-Text liegt weiterhin im Scratchpad, falls du ihn nachträglich anpassen willst — Bearbeiten geht dann über die GitHub-Oberfläche, `gh` kann einen abgegebenen Review nicht editieren. Wenn du später freigeben willst, ist das ein separater `gh pr review 76 --approve`.

---

## 7 — Nutzer

> Chatverlauf Wortgetreu als Protokoll nach Frank/Promps als md exportieren

### Claude

_[Aktion: Zielordner ist `Frank/Prompts` (nicht `Promps`); Konvention der Nachbardateien geprüft und bewusst zugunsten des wortgetreuen Wunsches abgewichen. Dieses Dokument ist das Ergebnis.]_

---

## Anhang — geposteter Review-Text (Wortlaut)

Sauberer PR — die Richtung stimmt und die Begründungen im Code sind nachvollziehbar. Besonders gut: dass der Drift-Check in beide Richtungen läuft und dass die Platzhalter ehrlich `501` liefern statt einer erfundenen Antwort. Dass Swagger/ReDoc/`openapi.json` bis hierher unauthentifiziert offen standen, war ein echter Fund.

**Kein Blocker.** Die fünf Punkte unten sind alle unkritisch — keiner bricht heute etwas, alle sind latent. Drei davon sind Einzeiler und wären in diesem PR billig mitgenommen; bei zwei würde ich einen Kommentar setzen und das Restrisiko bewusst akzeptieren.

### Was ich nachgestellt habe

- Backend-Suite auf dem Branch im `src-api`-Image: **124 Tests grün**, die Angaben im PR-Text stimmen.
- Befund 1 als Mutation: `Route` auf einem eingebundenen Router → `GET /internal/reindex` liefert **200 ohne Auth**, im selben Lauf bleiben alle 124 Tests grün.
- Befund 4 mit der Regex-Kette aus `check-api-types.mjs` gegen ein `client.ts` mit `//` im String: **0 Treffer** trotz echtem nackten `fetch(`; Gegenprobe ohne `//`: 1 Treffer.
- Befund 3 in zwei Container-Läufen mit dem benannten Volume: nach einem Lockfile-Bump auf 7.14.0 meldet der Container weiterhin **installiert 7.13.0**, `npm ci` wird übersprungen.

Nicht nachgestellt: Befund 2 und 5 sind direkte Code-Lesungen, da gab es keine Hypothese zu testen.

### In diesem PR mitnehmen (Einzeiler)

**1. `ChatView.tsx:51` — „✓ Gespeichert" auch bei fehlgeschlagenem Schreiben.** `.catch` schluckt die Ablehnung, `setParamSaved(true)` läuft unbedingt. Mit dem neuen `501`-Platzhalter auf `PUT /api/admin/config` ist das ab sofort garantiert: Admin ändert eine Schwelle, sieht das grüne ✓, die `config`-Tabelle bleibt unverändert. Ab T-37 wird daraus ein Betrieb mit angenommener, aber nicht gesetzter Fail-closed-Schwelle (ADR-008). Die Zeile ist von diesem PR nicht verändert — aber es ist genau das Fehlerbild, das ihr in `MessageBubble` drei Dateien weiter bewusst behoben habt. `try/catch` und `setParamSaved(true)` nur im Erfolgsfall.

**2. `MessageBubble.tsx:353` — Sperre gegen Doppel-Absendung ist weggefallen.** Dass `setFeedback` jetzt hinter dem `await` steht, ist richtig — es behebt das Latchen auf einen nie gespeicherten Wert. Es entfernt aber die zweite Eigenschaft der alten Reihenfolge: Während der Anfrage ist `feedback` weiterhin `null`, die Guard-Bedingung greift nicht. Heute folgenlos wegen `501`; ab T-30 zwei Zeilen zur selben `answer_id`, bei 👍/👎 mit widersprüchlichem `helpful`. Ein eigenes `submitting`-Flag hält beide Eigenschaften.

**3. `check-api-types.mjs:71` — der Bare-fetch-Wächter kann sich selbst blind machen.** Kommentare werden vor String-Literalen entfernt; ein `//` in einem String schneidet die Zeile inklusive schliessendem Anführungszeichen ab, die Quote-Paarung paart danach falsch und leert einen Bereich der Datei. Begrenzte Folge, weil `tsc` + openapi-fetch die Pfade weiterhin typisieren — es fällt nur die zweite Schicht weg. Fix: die beiden `.replace`-Zeilen tauschen.

### Kommentieren und Restrisiko akzeptieren

**4. `test_rbac.py:223` — der Routen-Wächter sieht nur die oberste Ebene.** Nachgestellt mit `router.routes.append(Route("/internal/reindex", …))`: ausgeliefert, `200` ohne Auth, nicht in der Spec, Suite grün. Die saubere Lösung ist Aufwand, die Eintrittswahrscheinlichkeit gering. Vorschlag: Restrisiko akzeptieren, aber den Docstring ehrlich machen — er verspricht „Nothing reaches the network that the two checks below cannot see", und das stimmt nachweislich nur für die oberste Ebene.

**5. `Makefile:64` — `generate-api` installiert nach einem Lockfile-Wechsel nicht neu.** Die Bedingung prüft die Existenz der Binärdatei, nicht das Lockfile. Kein Produktionsrisiko, kostet eine Debugging-Runde beim nächsten Bump. Die Abwägung im Kommentar (34 s kalt vs. 5 s warm) ist nachvollziehbar. Vorschlag: Restrisiko akzeptieren und den Ausweg in den Makefile-Kommentar schreiben (`docker volume rm learnflow-fe-node-modules`).

### Geprüft und in Ordnung

- `root_path="/api"` ist mit nginx' `rewrite ^/api/(.*)$ /$1` und mit dem direkten Healthcheck auf `:8000/health` unkritisch.
- `EXPOSE_API_DOCS` erreicht den Container über `env_file: .env`; die README-Anweisung stimmt.
- `FeedbackRequest` passt zur Tabelle `feedback` (`helpful` / `category` / `comment`), inklusive Nullbarkeit.
- openapi-fetch entfernt `Content-Type` bei einem `FormData`-Serializer — der Multipart-Upload behält seine Boundary.
- Der Zweig `!result.response.ok` in `unwrap` fängt korrekt den Fall ab, in dem openapi-fetch bei leerem Fehler-Body `error: undefined` liefert.

Zu den beiden offenen Fragen aus dem PR-Text (Antwortvertrag für `/query`, `PUT` statt `PATCH` bei T-37) habe ich keinen Einwand — beides ist konsistent mit ADR-008 und der ERD.
