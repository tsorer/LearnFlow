# Chatverlauf — Review PR #116 (T-54, RRF-Herkunft der Kontext-Chunks)

**Datum:** 2026-09-08
**Branch:** `main` (reiner Review-Vorgang, kein eigener Feature-Branch; PR #116 war zu diesem Zeitpunkt bereits gemergt — sichtbar im `git log`: `1449513 feat/T-54-rrf-herkunft: ... (#116)`).
**Gegenstand:** `/code-review`-Durchlauf (medium effort) auf [PR #116](https://github.com/tsorer/LearnFlow/pull/116), Konsolidierung mit den Befunden eines zweiten, unabhängigen Review-Prozesses, und das Posten des konsolidierten Ergebnisses als GitHub-Kommentar.
**Ergebnis:** 6 konsolidierte Findings (1 bestätigter Bug, 2 plausible Edge-Cases, 3 Cleanup-/Test-Findings), als Kommentar auf PR #116 gepostet: https://github.com/tsorer/LearnFlow/pull/116#issuecomment-5550620899

> Rekonstruktion des Verlaufs. Nutzer-Prompts wörtlich, Claude-Antworten als Zusammenfassung der Aussagen und durchgeführten Aktionen.

---

## 1 — Nutzer

> mach ein Review von https://github.com/tsorer/LearnFlow/pull/116 (T-54)

### Claude

`/code-review`-Skill mit dem PR-Link als Ziel aufgerufen (medium effort → 3 Korrektheits- + 3 Cleanup- + 1 Altitude- + 1 Conventions-Angle × bis 6 Kandidaten, 1-Vote-Verify, ≤ 8 Findings). Diff via `gh pr diff 116 --repo tsorer/LearnFlow` geholt (1251 Zeilen: neue Referenzdoku `Docs/09_RAG-Pipeline-Referenz.md`, `query.py`/`openapi.yaml`/`schema.d.ts`-Erweiterung um `chunk_id`/`document_id`/`sparse_rank`/`rrf_score` auf `ChunkDebugInfo`, Backend- und Frontend-Tests, umfangreicher Umbau der Chunk-Zeile in `MessageBubble.tsx`) vollständig gelesen.

Vier Finder-Agents parallel im Hintergrund gestartet (Angle A: Line-by-Line-Scan, Angle B: Removed-Behavior-Auditor, Angle C: Cross-File-Tracer, Cleanup: Reuse/Simplification/Efficiency), Altitude/Conventions-Check selbst durchgeführt (Abgleich gegen `CLAUDE.md` — keine Regelverstösse gefunden; `.sr-only`-Idiom als bestehende Konvention aus `QuizCard.tsx`/`DocumentViewer.tsx` bestätigt). Nach Eintreffen aller vier Agent-Ergebnisse dedupliziert und verifiziert:

- **Bestätigt:** `MessageBubble.tsx` — `chunk.rrf_score.toFixed(4).replace(/^0/, "")` entfernt nur eine führende `"0"`; bei `rrf_k = 1` (im Admin-Panel erlaubtes Minimum) und einem beidseitig auf Rang 1 gefundenen Chunk ergibt sich `rrf_score = 1.0` exakt → Anzeige `"1.0000"` statt des vorgesehenen kompakten `.xxxx`-Formats.
- Vier weitere reale, aber geringer schwere Findings: `term`/`defRow` nicht wie die übrigen Spalten-Konstanten auf Modulebene gehoistet; `RANK_ABSENT` im Frontend als nacktes Literal dupliziert statt aus dem generierten Schema abgeleitet; Test-Fixture „cut" mit `rrf_score`, der nicht zu ihrem `dense_rank` passt; Rundung von `rrf_score` auf 6 Stellen kann die in der Spec dokumentierte Tie-Break-Aussage („dann entscheidet `score`") an der Präzisionsgrenze unterlaufen.
- Verworfen nach Prüfung: `origin`-Fallback-Fehlbeschriftung bei „von keiner Suche gefunden" (durch `fuse()` strukturell unerreichbar) und „`chunk_id`/`document_id` ungenutzt" (laut PR-Beschreibung/Docstring bewusst als Vorgriff gedacht, kein Versäumnis).

Ergebnis über `ReportFindings` gemeldet, danach als Fliesstext zusammengefasst.

---

## 2 — Nutzer

> der andere Review Prozess hat folgendes gefunden:
>
> Review: PR #116 — RRF-Herkunft der Kontext-Chunks
> [...] Ein Finding überlebte die Verifikation: `MessageBubble.tsx:292` — `COL_RANK` ist eine feste 20px-Spalte für `dense_rank`/`sparse_rank`, aber `retrieval_top_k` ist laut PR-Beschreibung selbst bis 100 einstellbar. Ein dreistelliger Rang hat keine Overflow-/Whitespace-Behandlung und kann optisch mit der Nachbarspalte kollidieren.
>
> Konsolidiere sie und fasse sie deutsch zusammen

### Claude

Das zusätzliche Finding gegen den eigenen Fund-Katalog abgeglichen — kein Überlapp, als plausible sechste Position eingeordnet. Konsolidierte deutsche Zusammenfassung aller 6 Findings erstellt, nach Schweregrad geordnet (RRF-Anzeigefehler zuerst, dann Spaltenbreite, dann die drei Cleanup-/Test-Findings, zuletzt der Rundungs-Edge-Case), inklusive der beiden verworfenen Kandidaten am Ende zur Transparenz.

---

## 3 — Nutzer

> bitte als Kommentar auf github posten

### Claude

Vor dem Posten (Publish-Aktion, explizite Freigabe nötig) den aktiven `gh`-Account geprüft (`FraMo80`) und per `AskUserQuestion` bestätigen lassen, dass unter diesem Account auf PR #116 gepostet werden soll.

---

## 4 — Nutzer

> Ja, posten

### Claude

Konsolidierten Kommentar als Markdown-Datei im Scratchpad geschrieben und via `gh pr comment 116 --repo tsorer/LearnFlow --body-file ...` gepostet: https://github.com/tsorer/LearnFlow/pull/116#issuecomment-5550620899

---

## 5 — Nutzer

> exportiere den Chatverlauf nach Frank/Prompts

### Claude

Bestehenden Export (`2026-09-07_T-22-Performance-Test-PR-121.md`) als Vorlage gelesen und dieses Dokument nach der dort etablierten Konvention erstellt.
