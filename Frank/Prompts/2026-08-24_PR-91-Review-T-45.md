# Chatverlauf — Review PR #91 (T-45 Rate-Limit für POST /api/query)

**Datum:** 2026-08-24
**Branch (Arbeitsverzeichnis):** `feat/T-15-document-versioning`, unverändert. Der Review-Gegenstand `feat/T-45-rate-limit-query` wurde ausschliesslich gelesen (`gh pr view`, `git diff origin/main...`, `git show <ref>:<pfad>`), nie ausgecheckt.
**Gegenstand:** [PR #91](https://github.com/tsorer/LearnFlow/pull/91) „feat/T-45-rate-limit-query: Rate-Limit für POST /api/query, pro Konto statt pro IP" (Issue [#79](https://github.com/tsorer/LearnFlow/issues/79), Sprint 5, US-01), Autor `luenlestra`
**Ergebnis:** Review als [Kommentar](https://github.com/tsorer/LearnFlow/pull/91#issuecomment-5397491737) gepostet; Verdikt „approvebar nach drei kleinen Korrekturen, keine Blocker". Approve noch offen bis zur Umsetzung.

> Rekonstruktion des Verlaufs. Nutzer-Prompts wörtlich, Claude-Antworten als Zusammenfassung der Aussagen und durchgeführten Aktionen.

---

## 1 — Nutzer

> Mach bitte ein Review von T-45 (https://github.com/tsorer/LearnFlow/pull/91)

### Claude

**Vorgehen:** PR-Metadaten, Commits und Diff via `gh` / `git diff origin/main...`; Issue #79 gelesen und die fünf AK gegen die Umsetzung gestellt. Gegengeprüft gegen `app/limiter.py`, `app/main.py`, `app/routers/query.py`, `app/auth/jwt.py` + `app/auth/dependencies.py` (Token-Dekodierung und Fehlerklassen auf beiden Pfaden), `openapi.yaml` (auch der bestehende `429` beim Login), `tests/conftest.py` (`limiter.reset()`), das ganze `e2e/`-Verzeichnis, `src/docker-compose.yml` und `src/frontend/nginx.conf`. Ein Faktum im laufenden Stack statt aus dem Gedächtnis geprüft: `docker exec src-api-1 python -c "... limiter._headers_enabled"`.

**Verdikt: approvebar nach drei kleinen Korrekturen, keine Blocker.** Alle fünf AK erfüllt, CI grün auf `backend`/`frontend`/`e2e`, `mergeStateStatus: CLEAN`. Der `429` liegt vor dem Embedding-Aufruf — FastAPI löst die Dependencies auf, bevor der slowapi-Wrapper zählt, ein abgewiesener Request kostet nur die Auth-Query.

**Drei Befunde plus Kleinkram:**

1. **Medium — der Kommentar in `app/main.py:38` verspricht Header, die es nicht gibt.** Die neue Docstring begründet das Weglassen der Grenze aus dem Body damit, dass der maschinenlesbare Teil in den injizierten Headern reise. `limiter._headers_enabled` ist `False`, und `_inject_headers` prüft genau dieses Flag zuerst — die `429` trägt kein `RateLimit-*` und kein `Retry-After`. Der PR-Body sagt das unter „Nicht gelöst, bewusst" selbst; der Code-Kommentar widerspricht ihm. Vorschlag: `headers_enabled=True` am Limiter (ein Argument, macht den Kommentar wahr und ersetzt die im Frontend hartkodierte „eine Minute") oder minimal den Kommentar korrigieren.
2. **Medium — das e2e-Login-Budget ist nach dem PR bei 4 von 5, und T-15 bringt den fünften.** Login-Requests pro Lauf auf dem PR-Branch gezählt: `test_login_flow`, `test_documents_cascade`, `test_admin_config_endpoint` und neu `test_query_rate_limit` = 4, gegen `5/minute` pro IP mit einem Fenster, das den Lauf überlebt. `feat/T-15-document-versioning` bringt `e2e/test_documents_replace.py` mit — macht 5 und stellt einen einzelnen Lauf exakt auf die Kante. Es gibt noch kein `e2e/conftest.py`; ein session-scoped Token dort zöge alle Module auf einen Login zusammen.
3. **Minor — „the only endpoint that spends provider money" (`app/routers/query.py:94`) stimmt nicht.** `POST /api/documents` legt über `enqueue_document` einen Job an, `worker/main.py:94` embedded alle Chunks eines bis zu 10 MB grossen Dokuments. Der Unterschied ist nicht kostenlos vs. kostenpflichtig, sondern die Rolle: Upload verlangt `require_knowledge_owner`, Fragen darf jede Lernende. Betrifft nur den Kommentar, nicht das Doc.

**Kleinkram:** `assert len(statuses) <= LIMIT + 1` im e2e-Test ist durch das `range(LIMIT + 1)` garantiert und kann nie fehlschlagen · `tests/test_rate_limit.py` importiert `make_db`/`make_user` aus `tests.test_query`, der erste Test-zu-Test-Import im Backend-Suite · ein abgewiesener `429` hinterlässt weder `QuerySession` noch Logzeile, Missbrauch ist nirgends sichtbar.

**Als gut benannt:** Entscheid an drei Stellen begründet statt nur im PR-Text (Docstring, Konstante, Doc), Abgrenzung zum Login direkt unter dem Login-Entscheid, der Test „zweites Konto erbt das Fenster des ersten nicht" mit echtem Token über `account_key`, `detail`-Schema statt slowapis `{"error": …}`, Frontend mappt über den Status statt den Servertext zu spiegeln, und die DB-Nachkontrolle (alle 20 Probe-Fragen `suppressed`, kein LLM-Aufruf).

**Zwei Verdachtsmomente, die sich beim Nachprüfen auflösten und deshalb nicht in den Kommentar kamen:** (1) Der Fallback-Zweig in `account_key` ist tatsächlich tot, weil die Dependencies vor dem Endpoint laufen. (2) `--forwarded-allow-ips='*'` am uvicorn liesse `X-Forwarded-For` grundsätzlich spoofbar — `frontend/nginx.conf:46` setzt den Header aber überschreibend auf `$remote_addr` statt ihn anzuhängen, das IP-Keying des Logins ist also dicht. Ausserdem ist der Verzicht auf einen `config`-Eintrag unkritischer als der PR selbst behauptet: `Docs/03_QualityAttributes.md:83` zählt die betroffenen Schwellenwerte einzeln auf, ein Frequenzlimit ist nicht darunter.

**Eine Rückfrage statt eines Befunds:** Eine Notiz vom 15.08.2026 hielt fest, das Team habe im Zuge von T-45/#79 bereits **pro Client-IP** entschieden und wolle das nicht neu aufmachen. Issue #79 lässt den Schlüssel dagegen ausdrücklich offen, und im Repo steht der Entscheid nirgends. Der PR dreht ihn um, ohne zu erwähnen, dass er etwas umdreht — daher die Frage, ob die Notiz stimmt oder korrigiert gehört.

---

## 2 — Nutzer

> ja, poste das als Kommentar. Zu der Notiz: Ich hatte damal das Login im Hinterkopf und nicht nicht auf alles. So passt es für mich.

### Claude

Als Issue-Kommentar gepostet: [#91 (comment)](https://github.com/tsorer/LearnFlow/pull/91#issuecomment-5397491737), ausdrücklich als normaler Kommentar und nicht als formales Review — `reviewDecision` bleibt leer, kein „Request changes". Die Rückfrage zur Notiz aus dem Kommentar entfernt, weil damit erledigt.

**Notiz korrigiert.** Aus „Rate-Limits immer pro Client-IP" wurde die tatsächliche Regel, nämlich zwei Fälle nebeneinander: `/api/auth/login` pro IP (Lockout-Argument, NAT bewusst in Kauf genommen), authentifizierte Endpoints wie `POST /api/query` pro Konto (T-45). Dazu die Anwendungsregel „nicht raten, sondern fragen, ob der Endpoint authentifiziert ist" und der Hinweis, dass Spoofing wegen der nginx-Konfiguration kein Gegenargument gegen den IP-Schlüssel ist.

---

## 3 — Nutzer

> bitte den Chatverlauf nach Frank/Prompts exportieren

### Claude

Bestehende Exporte in `Frank/Prompts/` als Vorlage gelesen (zuletzt `2026-08-23_PR-89-Review-T-37.md`) und dieses Dokument nach der dort etablierten Konvention erstellt. Kein Branch-Wechsel, kein Commit — beides war nicht verlangt.
