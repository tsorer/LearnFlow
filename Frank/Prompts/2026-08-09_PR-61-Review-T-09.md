# Chatverlauf — Review PR #61 (T-09 Login-Flow E2E)

**Datum:** 2026-08-09
**Branch (Arbeitsverzeichnis):** `feat/T-12-parsing-chunking` — Review-Gegenstand ist der fremde Branch `feat/T-09-login-flow-e2e`
**Gegenstand:** [PR #61](https://github.com/tsorer/LearnFlow/pull/61) „feat/T-09-login-flow-e2e: Login-Flow E2E gegen den laufenden Stack" (Autor: @luenlestra, Closes #16)
**Ergebnis:** Review-Kommentar auf dem PR — [#issuecomment-5230711541](https://github.com/tsorer/LearnFlow/pull/61#issuecomment-5230711541)

> Rekonstruktion des Verlaufs. Nutzer-Prompts wörtlich, Claude-Antworten als Zusammenfassung der Aussagen und durchgeführten Aktionen.

---

## 1 — Nutzer

> mach bitte ein Review von https://github.com/tsorer/LearnFlow/pull/61. Sind alle Akzeptanzkriterien erfüllt? Ist die Test-Abdeckung in Ordnung? Ist der Coding-Style in Ordnung?

### Claude

**Vorgehen (Skill `/review`):** PR-Metadaten und Diff via `gh`, Issue #16 (Akzeptanzkriterien) gelesen, PR-Branch geholt (`17f0241`, sitzt direkt auf `main`/`b721eaa` — kein Rebase nötig). Umgebenden Code vom PR-Branch gelesen statt aus dem lokalen Arbeitsverzeichnis: `routers/auth.py`, `auth/dependencies.py`, `auth/jwt.py`, `limiter.py`, `config.py`, `seed_users.py`, `docker-compose.yml`, `nginx.conf`, `.env.example`, `Dockerfile`, `Makefile`, `ci.yml`, `auth.test.tsx`, `RouteGuards.tsx`.

**Eigene Verifikation** statt Vertrauen auf den Test-Plan des PRs — beide neuen Suiten im laufenden `src-api-1` ausgeführt (Testdateien nach `/tmp/t09` kopiert, damit das Arbeitsverzeichnis unberührt bleibt):

| Prüfung | Ergebnis |
|---|---|
| `tests/test_auth_chain.py` | 10 passed |
| `e2e/test_login_flow.py` (via `http://webapp`) | 7 passed |
| `ruff check` (aus `/app`, Projektkonfig) | clean |
| `mypy --strict` über beide neuen Dateien | Success, no issues |
| `gh pr checks 61` | `backend` / `e2e` / `frontend` grün |

Nebenbefund zur Methodik: der erste Testlauf aus `/tmp` heraus meldete 10 `SKIPPED` (fremdes `rootdir` → `asyncio_mode=auto` griff nicht) und ein falsch-positives `I001` (aus `/tmp` ist `app` kein First-Party-Modul). Beides Artefakt des Aufbaus, mit `-c pyproject.toml --rootdir=/app` bzw. `-w /app` korrigiert.

**Akzeptanzkriterien:**

- **AK 1 (Happy-Path Login → geschützte Route)** — erfüllt, doppelt abgedeckt (in-process + E2E). Der 404-statt-401-Test auf `/api/documents/{id}` belegt Token-Akzeptanz ausserhalb des auth-Routers ohne Fixture-Dokument.
- **AK 2 (ohne Token → Login-Seite)** — erfüllt: 401 auf beiden geschützten Routen, SPA-Fallback auf `/login`, Umleitung selbst über `auth.test.tsx` aus T-08. Die vom Autor selbst genannte Lücke bleibt: ein *während* der Sitzung ablaufendes Token führt zu keinem Redirect (leerer `catch` in `ChatView`, `ProtectedRoute` sieht nur den React-State). Buchstabe der AK gedeckt, Geist zur Hälfte.
- **AK 3 (CI)** — erfüllt, Job `e2e` läuft bei Push und PR und ist grün. Ohne Branch Protection blockiert er aber keinen Merge.

**Test-Abdeckung:** solide, über dem Issue-Anspruch. `is_active` war bisher in keinem Test des Repos abgedeckt, jetzt in beiden Richtungen. Verbleibende Lücken (keine Blocker): 403-Pfad (`require_knowledge_owner`) nur mit überschriebenem `get_current_user`, kein E2E-Negativfall (falsches Passwort, wegen 5/min-Budget), `mypy app worker` erfasst weder `tests/` noch `e2e/`.

**Coding-Style:** passt zum Repo. Zwei Punkte für diesen PR — (1) `ci.yml`: `--wait --wait-timeout 300` deckelt nur das Symptom, `docker compose up -d --build --wait webapp` startet über `depends_on` nur `webapp → api → db` und lässt den Restart-Loop-Kandidaten `worker` aussen vor; (2) `_reset_state`, `MISSING_DOCUMENT_ID` und der AK-2-Docstring sind dupliziert → `tests/conftest.py`. Nachrangig: `FakeDb` löst über kompiliertes SQL auf (koppelt an SQLAlchemys Parameter-Benennung, scheitert aber laut statt still), `PASSWORD_HASH = asyncio.run(...)` auf Modulebene kostet ~0,3 s pro `pytest`-Lauf, im CI-Job `docker exec ... seed_users.py` statt `make seed`.

**Doku:** `CLAUDE.md` verweist für Details auf `Ops/09_CI-Runbook.md`; das Runbook kennt weiterhin nur zwei Job-Spalten und nennt `make check` / `make check-backend` — Targets, die es im Makefile nicht mehr gibt.

**Sicherheit:** unkritisch. Festes `JWT_SECRET` und `BCRYPT_ROUNDS=4` nur im Wegwerf-Stack der CI, kein Secret committet; `down` ohne `-v` wegen des global benannten `learnflow_pgdata` korrekt. Fürs Backlog: `GET /documents/{id}` hat keine Ownership-Prüfung — jeder authentifizierte User darf jedes Dokument lesen; der neue E2E-Test schreibt das implizit fest.

**Fazit:** Approve, sobald die beiden Follow-ups als Issues erfasst sind. Prüfung der offenen Issues ergab: es existiert weder eines für den 401-Redirect noch für Branch Protection (`e2e` als Required Check braucht @tsorer).

---

## Zwischenmeldung (System, keine Nutzereingabe)

Der im Hintergrund nachlaufende `mypy --strict` meldete `Success: no issues found in 2 source files` — die neuen Testdateien halten `--strict` also tatsächlich ein, eine Ausweitung von `mypy app worker` auf `tests`/`e2e` wäre für diesen PR kostenlos.

---

## 2 — Nutzer

> Schreib hiervon eine Kurzfassung als PR-Kommentar bei GIT

### Claude

- Kurzfassung verfasst (Verifikationstabelle, AK 1–3, Test-Lücken, zwei Style-Punkte für diesen PR, Doku, Sicherheit, Fazit) und via `gh pr comment 61 --body-file` gepostet.
- Bewusst als reiner Issue-Kommentar, **nicht** als formales Review mit Approve/Request-Changes — die Freigabe bleibt beim Nutzer.
- Ergebnis: <https://github.com/tsorer/LearnFlow/pull/61#issuecomment-5230711541>

---

## 3 — Nutzer

> Chatverlauf bitte nach Frank/Prompts als md-Datei exportieren

### Claude

- Diese Datei (`Frank/Prompts/2026-08-09_PR-61-Review-T-09.md`), Format an `2026-06-21_CLAUDE-md-Review.md` angelehnt.
- Liegt untracked im Arbeitsverzeichnis des Branches `feat/T-12-parsing-chunking` — nicht committet.
