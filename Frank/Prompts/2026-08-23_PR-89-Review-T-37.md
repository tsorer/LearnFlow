# Chatverlauf — Review PR #89 (T-37 FastAPI: GET/PUT /admin/config)

**Datum:** 2026-08-23
**Branch (Arbeitsverzeichnis):** zu Beginn `feat/T-23-T-25-T-26-konfidenz-pipeline`, während des Reviews vom Nutzer nebenbei auf `feat/T-15-document-versioning` gewechselt. Der Review-Gegenstand `feat/T-37-admin-config` wurde ausschliesslich gelesen (`gh pr view`, `git diff origin/main...`, `git show <ref>:<pfad>`), nie ausgecheckt.
**Gegenstand:** [PR #89](https://github.com/tsorer/LearnFlow/pull/89) „feat/T-37-admin-config: FastAPI GET/PUT /admin/config" (Issue [#44](https://github.com/tsorer/LearnFlow/issues/44), Sprint 5, US-11)
**Ergebnis:** Review als [Kommentar](https://github.com/tsorer/LearnFlow/pull/89#issuecomment-5381258773) gepostet; Autor setzte alle sieben Punkte in `8665268` um; PR [approved](https://github.com/tsorer/LearnFlow/pull/89) durch `FraMo80`; die verbleibende Frontend-Drift als [Kommentar auf #45](https://github.com/tsorer/LearnFlow/issues/45#issuecomment-5386061648) (T-38) erfasst.

> Rekonstruktion des Verlaufs. Nutzer-Prompts wörtlich, Claude-Antworten als Zusammenfassung der Aussagen und durchgeführten Aktionen.

---

## 1 — Nutzer

> mach bitte ein Review von https://github.com/tsorer/LearnFlow/pull/89 (T-37)

### Claude

**Vorgehen:** PR-Metadaten und Diff via `gh` / `git diff origin/main...`; Issue #44 gelesen; gegengeprüft gegen die Migrationen `0009_config_threshold_constraints.py` und `0012_config_pipeline_constraints.py` (CHECK-Constraint, deferred Band-Order-Trigger, Regexes), `app/services/config.py` (`CONFIDENCE_THRESHOLD_KEYS` / `PIPELINE_KEYS`), `openapi.yaml`, `tests/conftest.py`, `tests/test_rbac.py` (Spec↔Code-Sync in beide Richtungen), `app/database.py`, `app/auth/dependencies.py`, das `Config`-Modell — und, ausschlaggebend, gegen den einzigen Aufrufer: `ChatView.tsx` und `api/client.ts`.

**Verdikt: Änderungen nötig.** Der Endpoint selbst sauber gebaut, aber inkompatibel mit seinem Aufrufer und der Vertrag nicht nachgezogen.

**Sieben Befunde, zwei davon blockierend:**

1. **Blocker — der Admin-Dialog kann nach dem PR nie mehr speichern.** `ChatView.tsx:43` lädt via `getConfig` die *ganze* Tabelle in `params`, `ChatView.tsx:55` schickt genau dieses Objekt unverändert zurück. Zusammen mit dem bewussten All-or-Nothing-422 enthält jeder Speichervorgang mindestens `chunk_size`, `chunk_overlap`, `stale_days` — aus dem GET zurückgespiegelt — plus je nach Bedienung `top_k`/`top_n` aus `PARAM_DEFS`, die es backendseitig gar nicht gibt (die Keys heissen `retrieval_top_k`/`context_top_n`). Ergebnis: immer 422, nie ein Write. Die AK „Änderung wirkt sofort ohne Neustart" damit end-to-end nicht erfüllt — genau der Befund, den das im PR-Body offen gelassene „Akzeptanzkriterien manuell durchgespielt" geliefert hätte. Vorschlag: einen nicht schreibbaren Key nur bei echter Wertabweichung ablehnen.
2. **Blocker — `openapi.yaml` nicht nachgezogen.** Die beiden `501`-Responses (`openapi.yaml:340` und `:383`) stehen noch drin, inklusive `description: Noch nicht implementiert (T-37)` — die einzigen zwei `501` im ganzen Spec. Die PR-Aussage „Keine Änderung an `openapi.yaml` nötig" stimmt also nicht, und die AK verlangt wörtlich den Ersatz des Platzhalters für beide Methoden. Folgeschäden: `schema.d.ts` behält den 501-Zweig, `ChatView.tsx:64` hat eine tote 501-Sonderbehandlung, `write-confirmation.test.tsx:93` testet dieses tote Verhalten grün.
3. **Major — `admin.py` war die einzige 501-Vorlage im Repo.** `tests/test_rbac.py:305` und CLAUDE.md verweisen beide darauf als Muster für Platzhalter-Routen; nach dem PR existiert im gesamten Code kein `501` mehr.
4. **Medium — `except IntegrityError` zu breit.** Jeder IntegrityError wurde zu 422 mit rohem DB-Text. Der Band-Order-Trigger ist der gewollte Fall; ein FK-Verstoss auf `changed_by` ist ein 500, kein Client-Fehler, und die Antwort leakt Postgres-Interna.
5. **Medium — die 422-Meldung nur gegen Mock getestet.** Der Test setzt `orig = Exception(...)`; real ist `exc.orig` der SQLAlchemy-asyncpg-Adapterfehler. Die gesamte Write-Semantik — inklusive „Mutieren des attached ORM-Objekts flusht ein UPDATE" — hing an `AsyncMock`. `e2e/test_config_threshold_constraints.py` existiert bereits als Muster für „gegen die echte Tabelle".
6. **Minor — `re.match` + `$` akzeptiert ein abschliessendes `\n`.** Der Kommentar behauptet „Mirrors migration 0012's regexes exactly"; Pythons `$` matcht auch vor einem finalen Newline, Postgres' `~` nicht. `fullmatch` statt `match`.
7. **Minor — der Whitelist-Entscheid stand nur im PR-Text.** Issue #44 sagt „Offen und hier zu entscheiden"; der Entscheid lebte in einem Code-Kommentar und der PR-Beschreibung, nicht im Issue und nicht in `Docs/`.

**Als gut benannt:** Validierung komplett vor dem ersten Write (kein Teil-Apply), Regex- und Key-Mengen exakt deckungsgleich mit `0012`, Audit-Felder gefüllt, GET zeigt bewusst auch das Nicht-Schreibbare, 401/403 für beide Methoden abgedeckt, Begründungen im Code statt nur im PR. Nebenbei geprüft: die automatische Erweiterung von `WRITABLE_KEYS` über `PIPELINE_KEYS` verträgt sich sauber mit dem T-25-Branch, dessen Migration `0013` CHECK-Regel und eigenen Band-Order-Trigger für `self_check_band_low/high` mitbringt.

Rückfrage, ob das als PR-Kommentar gepostet werden soll.

---

## 2 — Nutzer

> ja, poste das als Kommentar

### Claude

Als Issue-Kommentar gepostet: [#89 (comment)](https://github.com/tsorer/LearnFlow/pull/89#issuecomment-5381258773), Autor `FraMo80`. Ausdrücklich als normaler Kommentar, nicht als formales Review — `reviewDecision` blieb leer, kein „Request changes" gesetzt. Angebot, das bei Bedarf via `gh pr review 89 --request-changes` nachzuziehen.

---

## 3 — Nutzer

> die Korrekturen wurden umgesetzt. Der PR kann nun approved werden, oder?

### Claude

Ist-Zustand zuerst selbst geprüft statt die Aussage zu übernehmen: `git fetch`, neuer Commit `8665268` „fix(T-37): address review feedback on admin config endpoint", CI grün auf allen drei Jobs (`backend`, `frontend`, `e2e`), Diff `6186e58..8665268` vollständig durchgegangen. Zusätzlich geprüft, ob das zwischenzeitlich auf `main` gelandete T-31 (`e396382`) die berührten Dateien anfasst — tut es nicht.

**Alle sieben Befunde umgesetzt:**

- Befund 1: `PUT` liest die aktuellen Werte der übergebenen Keys vor der Validierung; ein nicht schreibbarer Key ist nur noch bei echter Wertabweichung ein Fehler, ein unbekannter Key (keine Zeile) immer. Abgedeckt in Unit- **und** e2e-Test.
- Befund 2: beide `501` raus, `schema.d.ts` regeneriert, tote 501-Zweige in `ChatView.tsx` und `write-confirmation.test.tsx` mit entfernt; die PUT-Beschreibung dokumentiert jetzt die Pass-Through-Regel.
- Befund 3: CLAUDE.md und `test_rbac.py:305` beschreiben die Platzhalter-Form inline statt auf `admin.py` zu zeigen.
- Befund 4: auf `sqlstate == "23514"` eingegrenzt, sonst `raise`; eigener Test dafür.
- Befund 5: neues `e2e/test_admin_config_endpoint.py` gegen den laufenden Stack — echter Write sofort sichtbar, Band-Order-Trigger über HTTP erreichbar, Round-Trip-Fix gegen die Live-Tabelle. `assert "darf nicht über" in detail` belegt, dass `.sqlstate` am asyncpg-Adapter tatsächlich aufgeht.
- Befund 6: `fullmatch`, plus Test mit `"0.35\n"`.
- Befund 7: Nachtrag 2026-08-22 in ADR-008, inklusive Begründung der Pass-Through-Ausnahme.

Nebenbei: der Write-Pfad wurde von ORM-Mutation auf `update()`-Statements umgestellt, was das frühere „Zeile fehlt"-500 gegenstandslos macht.

**Zwei nicht blockierende Restpunkte benannt:** (1) die Docstrings im neuen e2e-Test sprechen noch von „mutating the attached ORM object" — seit `update()` nicht mehr zutreffend; (2) das Parameter-Panel rendert weiterhin Regler, die gegen die Whitelist zwangsläufig 422 erzeugen (`top_k`/`top_n` als Namensfehler, `chunk_size`/`chunk_overlap` als nicht schreibbar, `self_check_band_*` erst mit T-25) — Frontend-Drift von vor diesem PR, gehört zu T-38.

**Fazit: approvebar.** Angebot, das Approve zu setzen und für Punkt 2 ein Folge-Issue anzulegen.

---

## 4 — Nutzer

> Ist Punkt 2 nicht das, was wir letzte Woche bereits als Kommentar bei T-37 erfasst haben. Oder sollte es besser ein Kommentar bei T-38 sein? Wobei die beiden Tickets eh zusammen umgesetzt werden.

### Claude

Nachgeprüft (`gh issue view 44 --comments`, `gh issue view 45`): **teilweise.** Der bestehende Kommentar auf #44 deckt nur die `llm_*`-Regler ab (verschoben in ein eigenes Issue nach T-18) und schliesst mit „die übrigen Parameter im Panel funktionieren mit T-37 sofort" — was nach dem Whitelist-Entscheid dieses PRs nicht mehr stimmt. Nicht erfasst sind der Namensfehler `top_k`/`top_n`, die nicht schreibbaren `chunk_size`/`chunk_overlap` und die erst mit T-25 existierenden `self_check_band_*`.

Beim Lesen von #45 zusätzlich aufgefallen und als der eigentlich wichtige Teil benannt: **T-38s erste AK lautet „Formular für beide Schwellenwerte vorhanden" — für `confidence_threshold_high`/`_medium` gibt es im Panel überhaupt keinen Regler.** Ausgerechnet die zwei Keys, um die es in US-11 geht, sind nicht bedienbar (`rrf_k` fehlt ebenfalls). Und die zweite Hälfte derselben AK („Stale-Schwellenwert") kollidiert direkt mit dem Entscheid dieses PRs, `stale_days` read-only zu lassen.

**Empfehlung: Kommentar auf #45, nicht auf #44** — #44 wird mit dem Merge geschlossen, und Notizen auf geschlossenen Issues liest niemand mehr; #45 bleibt offen und ist das Ticket, das beim Bauen des Panels aufgeschlagen wird. Dass beide zusammen umgesetzt werden, spricht nur dagegen, ein drittes Issue aufzumachen. Den `llm_*`-Teil per Verweis auf #44 statt als Wiederholung.

---

## 5 — Nutzer

> ja, mach beides

### Claude

**Kommentar auf #45** gepostet ([issues/45#issuecomment-5386061648](https://github.com/tsorer/LearnFlow/issues/45#issuecomment-5386061648)): Tabelle Regler ↔ `config`-Zeile mit dem jeweiligen Verhalten beim Speichern, der Hinweis auf die All-or-Nothing-Semantik (ein *angefasster* Regler kippt die ganze Speicherung, ein unveränderter nicht), die beiden AK-Konflikte und der Verweis auf den `llm_*`-Kommentar in #44.

**PR #89 approved** (`gh pr review 89 --approve`), verifiziert: `reviewDecision: APPROVED`, Reviewer `FraMo80`. Der Approve-Text nennt die umgesetzten Punkte, hebt den e2e-Nachweis für `.sqlstate` hervor und führt die beiden nicht blockierenden Restpunkte mit Verweis auf #45 auf.

Abschliessend angemerkt: die DoD-Zeile „Review durch eine zweite Person" ist damit abgehakt (Autor ist tsorer); offen bleibt formal nur „Akzeptanzkriterien manuell durchgespielt im laufenden System", dessen Kern der neue e2e-Test inzwischen automatisiert abdeckt.

---

## 6 — Nutzer

> bitte den Chatverlauf nach Frank/Prompts exportieren

### Claude

Bestehende Exporte in `Frank/Prompts/` als Vorlage gelesen (zuletzt `2026-08-23_PR-88-Review-T-31.md`) und dieses Dokument nach der dort etablierten Konvention erstellt. Kein Branch-Wechsel, kein Commit — beides war nicht verlangt.
