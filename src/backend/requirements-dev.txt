# Werkzeuge fuer die CI-Checks (Lint, Typing, Tests).
# Versionen gepinnt, damit lokal und in CI exakt dasselbe laeuft.
ruff==0.6.9
mypy==1.11.2
types-python-jose==3.3.4
pytest==8.3.3
pytest-asyncio==0.23.8
httpx==0.27.2
openapi-spec-validator==0.7.1
pyyaml==6.0.3

# Nur fuer docker-compose.debug.yml (T-63). Der debug-Stack haengt sich an den
# api-Container, und der wird mit INSTALL_DEV=true gebaut -- das worker-Image
# (INSTALL_DEV=false) braucht den Debugger nicht.
debugpy==1.8.5
