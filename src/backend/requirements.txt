fastapi>=0.115.0
uvicorn[standard]>=0.30.0

# Database
asyncpg>=0.29.0
sqlalchemy[asyncio]>=2.0.0
alembic>=1.13.0
pgvector>=0.3.0

# Auth
python-jose[cryptography]>=3.3.0
# Direkt, nicht mehr transitiv ueber passlib[bcrypt] (T-63): app/auth/jwt.py
# importiert `bcrypt` selbst, passlib wurde nirgends benutzt. Ohne diese Zeile
# haette das Entfernen von passlib die Anmeldung mitgenommen.
bcrypt>=4.1.0
python-multipart>=0.0.9

# LLM / Embeddings
# Kein direktes `openai` (T-63): alle Aufrufe laufen ueber LiteLLM (ADR-004),
# das seinen Provider-SDK-Bedarf selbst mitbringt.
litellm>=1.40.0

# Background worker
pgqueuer>=0.14.0,<1.0.0

# Document parsing
# >=6.16.2: earlier versions compare a cm-scaled advance against an unscaled
# threshold and split words apart ("hochr iskant") in PDFs that put their scale
# in the cm matrix — see T-50 and tests/test_parsing.py.
pypdf>=6.16.2
python-docx>=1.0.0
tiktoken>=0.7.0

# Rate limiting
slowapi>=0.1.9

# Config
pydantic-settings>=2.3.0
python-dotenv>=1.0.0
